Parkrise.Header.Search = {
  init: function(){
    var that = this;
    that.searchAutoHeader();
  },
  searchAutoHeader: function(){
		$('.parkrise-ultimate-search').submit(function(e) {
			e.preventDefault();
			var q = $(this).find('input[name=q]').val();
			if(q.indexOf('script') > -1 || q.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).find('input[name=q]').val('');
			}
			else{
				var q_follow = 'product';
				var query = encodeURIComponent('(title:product contains ' + q + ')');
				if( !q ) {
					window.location = '/search?type='+ q_follow +'&q=*';
					return;
				}	
				else {
					window.location = '/search?type=' + q_follow +'&q=filter=' + query;
					return;
				}
			}
		});
		var $input = $('.parkrise-ultimate-search input[type="text"]');
		$input.bind('keyup change paste propertychange', function() {
			var key = $(this).val(),
					$parent = $(this).parents('.parkrise-wpo-wrapper-search'),
					$results = $(this).parents('.parkrise-wpo-wrapper-search').find('.parkrise-smart-search-wrapper');

			if(key.indexOf('script') > -1 || key.indexOf('>') > -1){
				alert('Từ khóa của bạn có chứa mã độc hại! Vui lòng nhập lại từ khóa khác');
				$(this).val('');
				$('.parkrise-ultimate-search input[type="text"]').val('');
			}
			else{
				if(key.length > 0 ){
					$(this).attr('data-history', key);
					$('.parkrise-ultimate-search input[type="text"]').val($(this).val());
					var q_follow = 'product',
							str = '';
					/*	str = '/search?q=filter=(title:product contains ' + key + ')&view=header-ultimate-product';*/
					str = '/search?type=product&q=' + key + '&view=header-ultimate-product';
					$.ajax({
						url: str,
						type: 'GET',
						async: true,
						success: function(data){
							$results.find('.parkrise-resultsContent').html(data);
						}
					})
					if(!$('.parkrise-header-action_search').hasClass('parkrise-js-action-show')){
						$('body').removeClass("parkrise-locked-scroll");
						$('.parkrise-header-action-item').removeClass('parkrise-js-action-show');
					}
					$(".parkrise-ultimate-search").addClass("parkrise-expanded");
					$results.fadeIn();
				}
				else{
					$('.parkrise-ultimate-search input[type="text"]').val($(this).val());
					$(".parkrise-ultimate-search").removeClass("parkrise-expanded");
					$results.fadeOut();
				}
			}

		})
		$('body').click(function(evt) {
			var target = evt.target;
			if (target.id !== 'parkrise-ajaxSearchResults' && target.id !== 'parkrise-inputSearchAuto') {
				$("#parkrise-ajaxSearchResults").hide();
			}
			if (target.id !== 'parkrise-ajaxSearchResults-mb' && target.id !== 'parkrise-inputSearchAuto-mb') {
				$("#parkrise-ajaxSearchResults-mb").hide();
			}
			if (target.id !== 'parkrise-ajaxSearchResults-3' && target.id !== 'parkrise-inputSearchAuto-3') {
				$("#parkrise-ajaxSearchResults-3").hide();
			}
		});
		$('body').on('click', '.parkrise-ultimate-search input[type="text"]', function() {
			if ($(this).is(":focus")) {
				if ($(this).val() != '') {
					$(".parkrise-ajaxSearchResults").show();
				}
			} else {
			}
		})
		$('body').on('click', '.parkrise-ultimate-search .parkrise-search-close', function(e){
			e.preventDefault();
			$(".parkrise-ajaxSearchResults").hide();
			$(".parkrise-ultimate-search").removeClass("parkrise-expanded");
			$(".parkrise-ultimate-search").find('input[name=q]').val('');
		})
	},
};

(function() {
  Parkrise.Header.Search.init();
})();
